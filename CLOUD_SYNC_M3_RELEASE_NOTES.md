# ZEZMS Cloud Sync M3 — Release Notes

Build: `20260722-m3-live-sync-r1`

This full release is based on the successfully deployed M2 package. It adds a Supabase-backed, authenticated, revision-controlled realtime synchronisation layer while preserving all M2 Google Drive backup and restore features.

Important: run `SUPABASE_SETUP_M3.sql` before attempting to connect the app.
